import { Routes } from '@angular/router';

import { Springbootresponse } from './springbootresponse/springbootresponse';

export const routes: Routes = [
  { path: '', component: Springbootresponse }
];
